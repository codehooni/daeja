import 'package:flutter/cupertino.dart';

EdgeInsets sheetPadding = EdgeInsets.only(
  top: 4.0,
  left: 16.0,
  right: 16.0,
  bottom: 48.0,
);

BorderRadius sheetBorderRadius = BorderRadius.vertical(
  top: Radius.circular(16.0),
);

BorderRadius borderRadius = BorderRadius.circular(12.0);
